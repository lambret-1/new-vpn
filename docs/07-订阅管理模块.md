# 07 - 订阅管理模块

## 7.1 职责

- 订阅源 CRUD
- 持久化 `subscriptions.json`
- 定时自动更新任务
- 手动触发更新，调用订阅解释器
- 保存上次更新时间、状态、meta 流量信息
- 展示流量、到期信息给 UI

## 7.2 数据模型

```swift
struct Subscription: Identifiable, Codable {
    let id: String
    var name: String
    var url: String
    var enable: Bool
    var autoUpdate: Bool
    var updateIntervalMin: Int      // 更新间隔，分钟
    var lastUpdateTime: Date?
    var lastUpdateStatus: UpdateStatus
    var lastErrorMessage: String?
    var metaInfo: SubscriptionMetaInfo
    var filterRule: SubscriptionFilterRule
}

enum UpdateStatus: String, Codable {
    case success
    case fail
    case pending
}

struct SubscriptionMetaInfo: Codable {
    var totalTrafficBytes: Int64    // 总流量
    var usedTrafficBytes: Int64     // 已用流量
    var expireTimestamp: Int64       // 到期时间戳，0 表示无
}

struct SubscriptionFilterRule: Codable {
    var excludeProtocol: [String]    // 排除协议
    var excludeKeyword: [String]     // 排除关键词
    var onlyIncludeKeyword: [String] // 只包含关键词
}
```

## 7.3 定时更新

- 后台定时协程
- 每个订阅独立配置更新间隔（15min - 24h）
- 程序启动后按 `lastUpdateTime + interval` 判断是否需要更新
- 程序后台驻留时执行
- 程序退出停止
- 自动更新失败：仅写日志，不弹窗
- 自动更新完成：不自动触发批量测速

## 7.4 手动更新

1. UI 点击更新
2. 调用订阅解释器拉取解析
3. 成功：提示成功，显示解析统计
4. 失败：弹窗显示错误，**不清除旧节点**

## 7.5 删除订阅两种模式

### 模式 A：保留节点

- 只删除订阅源配置
- 保留已导入节点
- 节点自动变为 `sourceType = manual`

### 模式 B：删除节点

- 删除订阅源
- 删除全部来自该订阅的节点

## 7.6 核心 API

```swift
final class SubscriptionManager {
    static let shared = SubscriptionManager()

    func loadSubscriptions() throws
    func addSubscription(_ sub: Subscription) throws
    func updateSubscription(_ sub: Subscription) throws
    func deleteSubscription(id: String, keepNodes: Bool) throws
    func refreshSubscription(id: String) async throws
    func refreshAll() async
    func startAutoUpdateTimer()
    func stopAutoUpdateTimer()
}
```

## 7.7 配置项

```json
"subscriber": {
  "fetchUseProxy": false,
  "httpTimeoutSec": 30,
  "maxRawContentSizeBytes": 2097152
}
```

- `fetchUseProxy`：拉取订阅是否走当前代理（默认 false，直连防止死锁）
- `httpTimeoutSec`：HTTP 超时
- `maxRawContentSizeBytes`：订阅内容最大限制（默认 2MB）
